%h2 Thank you for your order
- @cart.shopping_cart_items.each do |cart_item|
  = cart_item.item.name
  %p Total:
  = @cart.total
  SEK
%br
Rating
%br
= image_tag('rate_l.png')
= @rating.rating.round(2)
%br
= form_for [@rating], url: rate_path(cart_id: @cartid),method: 'post' do |f|
  = f.select(:rating, ['1','2','3','4','5'])
  = f.submit "Rating"
